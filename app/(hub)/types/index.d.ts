// this is the main hub type declarations
// here must be declared general types used in the hub for all site

// the type architecture is baed on feature-driven architecture so inside every section is a type declaration for section itself

// Here will manage ids fot sections

export type SectionsId = 'home' | 'project' | 'team' | 'contact'
